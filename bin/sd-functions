#!/usr/bin/env bash

# sdir wrapper
function sdir() {
    local SDIR_BIN=$(which sdir)

    if [[ ! "$@" = "-" ]]; then
        $SDIR_BIN $@
    fi

    dir=$($SDIR_BIN -)
    if [ -n "$dir" ]; then
        cd $dir
    fi
}
